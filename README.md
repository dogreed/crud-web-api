# crud web api
1. Hosted on IIS server
2. I just activated IIS in control paneel
3. open inetmgr and then released file in desktop
4. copied entire thing {one that was published into folder} in intmger - create a new folder - then create in a default pool (we can make own pool)
5. after that downloaded  Windows Hosting Bundle Installer
6. after that configured settings connected apollo databse to ms sql
7. Now it worked in local network
